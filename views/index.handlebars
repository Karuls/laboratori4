<h1>Телефонный справочник</h1>

<div class="contacts-list">
    {{#each contacts}}
        <a href="/Update?id={{this.id}}" class="contact-btn">{{this.name}} - {{this.phone}}</a>
    {{/each}}
</div>

<div class="add-btn-container">
    <a href="/Add" class="btn btn-primary">Добавить</a>
</div>